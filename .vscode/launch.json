{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "barnes-hut",
            "program": "${workspaceFolder}/barnes-hut",
            "args": [
                "-t",
                "0"
            ],
            "cwd": "${workspaceFolder}",
            "expressions": "native"
        }
    ]
}